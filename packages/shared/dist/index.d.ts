export { Candle, Money, Position, PositionJSON, Direction, MarketParameters, FrequencyTypes, PeriodToFrequencyTypes, PeriodTypes } from "./types";
export { Strategy, BacktestResult } from "./strategy";
export interface RedirectURL {
    url: string;
}
export interface ResStatus {
    status: string;
}
export interface User {
    user: string;
}
//# sourceMappingURL=index.d.ts.map